from pytime import *

def temporizador(h, m, s):
    while n!=0:
        n-=1
        time.sleep(n)
        if m!=0 & n=0:
            m-=1
            if h!=0 & m=0:
                h-=0
        print(f"{h}:{m}:{s}")

hora, min, seg=int(input("Dime el tiempo::: ")).split(:)
temporizador(hora, min, seg)
